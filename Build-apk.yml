name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Create main.py (WebView launcher)
        run: |
          cat > main.py << 'EOF'
          from kivy.app import App
          from kivy.uix.webview import WebView
          from kivy.clock import Clock
          import threading
          import subprocess
          import os

          class TradingBotApp(App):
              def build(self):
                  # Start the trading bot in background
                  threading.Thread(target=self.start_bot, daemon=True).start()
                  # Wait a few seconds for the HTTP server to start
                  Clock.schedule_once(lambda dt: None, 4)
                  return WebView(url='http://localhost:8888')
              
              def start_bot(self):
                  os.chdir(os.path.dirname(os.path.abspath(__file__)))
                  # Run nostop.py (which starts the server on port 8888)
                  subprocess.run(['python', 'nostop.py'], check=False)

          if __name__ == '__main__':
              TradingBotApp().run()
          EOF

      - name: Create buildozer.spec
        run: |
          buildozer init
          sed -i \
            -e 's/^title = .*/title = Trading Bot/' \
            -e 's/^package.name = .*/package.name = tradingbot/' \
            -e 's/^package.domain = .*/package.domain = org.tradingbot/' \
            -e 's/^version = .*/version = 0.1/' \
            -e 's/^requirements = .*/requirements = python3,kivy,pandas,numpy,yfinance,requests/' \
            -e 's/^android.permissions = .*/android.permissions = INTERNET, ACCESS_NETWORK_STATE, WAKE_LOCK, FOREGROUND_SERVICE/' \
            -e 's/^#*android.bootstrap = .*/android.bootstrap = webview/' \
            -e 's/^orientation = .*/orientation = landscape/' \
            -e 's/^fullscreen = .*/fullscreen = 0/' \
            buildozer.spec
          # Additional important settings
          echo "android.api = 30" >> buildozer.spec
          echo "android.minapi = 21" >> buildozer.spec
          echo "android.ndk = 25b" >> buildozer.spec
          echo "android.sdk = 30" >> buildozer.spec
          echo "android.gradle_version = 8.0" >> buildozer.spec
          echo "android.archs = arm64-v8a, armeabi-v7a" >> buildozer.spec
          # Prevent timeouts
          echo "buildozer.log_level = 2" >> buildozer.spec

      - name: Build APK using Docker action
        uses: kivy/buildozer-action@v1
        with:
          command: buildozer android debug
          workdir: .

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: trading-bot-apk
          path: bin/*.apk
